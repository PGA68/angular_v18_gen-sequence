import { DirectiveName4libDirective } from './directive-name4lib.directive';

describe('DirectiveName4libDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveName4libDirective();
    expect(directive).toBeTruthy();
  });
});
